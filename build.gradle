allprojects {
    apply plugin: 'java'
    group = 'kusitms'

    sourceCompatibility = '17'

    repositories {
        mavenCentral()
    }
}

jar {
    enabled = false
}

tasks.named('test') {
    useJUnitPlatform()
}